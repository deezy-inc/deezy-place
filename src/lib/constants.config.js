/**
 * This config file is used to override the default config file from nosft-core
 * You can add your own specific nosft configs as well.
 */
export const localConfig = {
    NOSTR_RELAY_URL: "wss://nostr.openordex.org",
};
